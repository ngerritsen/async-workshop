const groceryService = require('../lib/groceryService');

const NEW_ITEM_NAME = 'Coffee';

/**
 * Exercise 2.
 *
 * Set a a new item with the name above, then get it back from the service and log it
 *
 * - You have to call the service with the callback API, you cannot use promises at all.
 * - You do not have to handle errors.
 */
